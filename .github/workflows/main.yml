name: Deploy mdbook to GitHub Pages

on:
  push:
    branches: [zh]

permissions:
  contents: write

jobs:
  deploy:
    uses: ./.github/workflows/build.yml
    with:
      site_url: /crp-zh/
      destination_dir: ""
